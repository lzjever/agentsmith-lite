import { forwardRef, type ButtonHTMLAttributes } from "react";
import { cn } from "./cn";

export const Switch = forwardRef<HTMLButtonElement, Omit<ButtonHTMLAttributes<HTMLButtonElement>, "onChange"> & { checked?: boolean; onCheckedChange?: (checked: boolean) => void }>(function Switch({ checked = false, onCheckedChange, className, disabled, ...props }, ref) { return <button ref={ref} type="button" role="switch" aria-checked={checked} data-state={checked ? "checked" : "unchecked"} disabled={disabled} onClick={() => onCheckedChange?.(!checked)} className={cn("peer inline-flex h-5 w-9 shrink-0 cursor-pointer items-center rounded-pill border border-border-input/80 bg-input transition-colors duration-150 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent/25 focus-visible:ring-offset-2 focus-visible:ring-offset-background disabled:cursor-not-allowed disabled:opacity-50 data-[state=checked]:border-accent data-[state=checked]:bg-accent", className)} {...props}><span data-state={checked ? "checked" : "unchecked"} className="pointer-events-none block size-4 translate-x-0 rounded-full bg-white shadow-ambient transition-transform duration-150 data-[state=checked]:translate-x-4" /></button>; });
